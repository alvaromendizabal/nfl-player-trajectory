# Feature research ledger

| Avenue | Evidence | Current decision |
|---|---|---|
| Arrival response in a small ridge diagnostic | Earlier pooled gain; component omission evidence | No universal retention: transfer to the later tree diagnostic did not pass |
| Earlier-origin ridge mixture | Worsened all inspected folds | Stop that specific mixture/interface unchanged; not a rejection of sequence-model augmentation |
| Turning, braking, orientation additions | Earlier fixed diagnostic gates failed | Stop those definitions unchanged |
| Terminal and synchronized relationship summaries | Fixed-tree additions did not pass | Stop that interface; learned time/player interactions remain untested here |
| Direct observed state | First-fold gain; later two-fold replication negative | Do not retain the broad bundle in this diagnostic |
| Passing-line state and history | Round 6 completed; both lost to control pooled | Stop unchanged; beating an inferior intermediate is insufficient |
| Ordered role/cohort and player histories | Maintained temporal-model protocol provides separate precedent; diagnostic control does not contain fitted histories | Round 7: explicit chronological encoding and matched support ablation; new NFL effect unmeasured |
| Learned temporal/player representation | Strong solution authors preserve ordered observed sequences and player interactions | Next bounded representation integration/profiling milestone, rather than an endless series of tabular geometric variants |
| Full-scale integration and benchmark reproducibility | Historical 0.62708 model lineage remains distinct/incomplete in earlier project record | Recover/rebuild a replayable comparison before claiming gains to that system |
| Other permitted seasons | External alignment and availability assumptions not established by these kits | No new download or use without verified rules, event and role alignment |

## Concrete next branch

A positive role/player-history result earns integration into a replayable temporal forecasting path with the same availability/count channels and a no-outcome-statistics control. A negative result stops this hierarchical encoder unchanged. Either way, do not invent another passing-line variation or tune smoothing on these folds. Reserve a broader validation population before promoting improvements, and report reuse of every previously inspected game set.

Feature research is not complete. There is no supported numerical allocation of the leaderboard gap solely to features, architecture, training or ensembles. The 0.46340 reference is the user's historical research target, not a newly verified leaderboard lookup in this milestone and not an attained result.

## Publication

Round 7 is delivered for manual execution outside the Git checkout. No automatic commit, public notebook export, AWS change or Kaggle submission occurs. After reviewing actual results, consolidate tested source and public aggregate experiment records into the canonical repository; keep IDs, private tables, weights, raw tracking and per-row forecasts out of Git. Do not use `git add .` on results or blindly publish executed private notebooks.
