# Research ledger

## Current decision

Round 8's history arm scored 0.764215946953194 versus matched terminal 0.7641371703104738 and preserved tree 0.7204400369653067 on 5,886 rows / 14 reused evaluation games. Its feature gate and later-fold gate both failed. The tiny arm difference does not prove that the pair branch is disconnected or that relational information is unhelpful in general. Do not repeat or scale this exact study unchanged.

Several earlier geometric and historical tree additions failed their later-game gates. Their rejection is conditional on the tested sample, estimator and interface; the feature space is not declared exhausted. The original large feature-bank and historical ~0.62708 model are distinct from these small manual diagnostic studies.

## Next bounded work

Round 9 diagnoses representation variation and frozen-model sensitivity and constructs an observed-only goal-frame prototype on 32 training plays. It performs no new fitting. The key missing evidence is whether the current relational path uses the different history information at all.

A following scientific milestone should be selected from the audit evidence, not scheduled automatically. Plausible directions include stronger query/role-conditioned relational representation, preserving peer identity through aggregation, and goal-frame representation with a matched same-capacity control. These change both the feature interface and its inductive assumptions and require explicit attribution.

## Do not conflate performance gaps

There is no controlled numerical decomposition of the remaining gap into features, optimizer, capacity, augmentation, sample coverage, or ensembling. Leading approaches use several of those components together. We will prioritize representation while keeping competing explanations testable. Neither the research target 0.46340 nor the approximate user-reported 0.62 is interchangeable with these reused-fold scores.

## Publication

The manual kits through Round 9 are separate local artifacts, not automatically committed or merged into the canonical repository. Current verified GitHub main: 402843faa1722460aa84d1bbaf27c4050a7b7ff9. A subsequent reviewed publication should include code/tests and sanitized aggregate evidence, not raw data, private tensors, keys, weights or unreviewed executed notebook outputs. No writes to GitHub are performed by this kit.
