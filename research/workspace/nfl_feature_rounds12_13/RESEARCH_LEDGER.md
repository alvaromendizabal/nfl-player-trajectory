# Continuing research ledger

## Complete and stopped unchanged

The historical tabular treatments and small neural tests remain recorded with
their original populations and gates. Round 10's goal features achieved a nominal
1.51% matched evaluation improvement but did not pass uncertainty or tree-parity
requirements. Round 11 added training-fit and coverage evidence, not new accuracy.
No previous treatment is promoted merely because training error improved.

## Deliver now: two independent full feature experiments

Round 12: direct receiver-history six-channel core plus six dynamic/goal-relative
extensions. Round 13: observed acceleration/turning six-channel core plus six
contiguous multiscale/jerk extensions. Each has mask, core and full arms, identical
capacity, complete checks, two notebooks, plots, profiles, checkpoints and replay.
No adaptive Round 12 -> Round 13 dependency.

## Highest-priority separate follow-through

Data scale: only 699/3,352 observed training-game plays are selected. Verify all
unused-play label schemas, request coverage and row identities first; do not label
them ready because their observed keys exist. Then freeze an expanded training
sample, hold evaluation games fixed, and compare only data volume using the same
model/features and an explicitly justified exposure budget. Do not confound this
with the two delivered feature contrasts.

Validation: stop another indefinite series of tweaks on these 14 games. Use the
existing chronological research protocol for subsequent stability checks; the
old reserved set was already inspected and is not pristine. No across-season
claim follows from calendar dates inside one labelled season.

Representation: study player-centric vs shared coordinates, full frame sequences,
forecast parameterization and relationships only with matched controls. Consider
training-data augmentation or auxiliary objectives separately; previous failed
implementations do not universally reject the concept but must not be rerun
unchanged. No blanket claim that algorithms are the only or never the bottleneck.

Publication/inference: keep model sources, feature lineage, exact row-key replay,
canonical notebooks and metrics together before promotion. Manual kits are not
already GitHub commits. Do not copy private arrays, executed private-play figures,
labels, histories, credentials or model weights to the public repository without
review. Track each accepted or rejected experiment in a reviewed code publication.

Ensembling is later: do not optimize weights against repeatedly inspected games
while claiming a feature-only improvement. A high-scoring system must also have
reproducible trained artifacts and identical offline/inference input contracts.
