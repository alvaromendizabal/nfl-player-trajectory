# Validation scope

**Prepared and statically inspected. Project code, tests, notebooks, model creation,
profiling and training have NOT been executed by the assistant.**

Completed during preparation: read the provided ZIP/JSON/notebook outputs;
compared reported content; inspected source; created files; parsed Python syntax
and notebook cell syntax without running it; generated checksums and archives.
These checks do not establish runtime correctness or numerical validity.

The uploaded user receipt establishes 110 passing tests for the ORIGINAL 14/15
scientific source. It does not validate the newly prepared installer, presentation
changes, or 16/17 feature/model integration. The original scientific sources are
not patched. Installer regression tests and new-study tests are included for the
user to run; no successful results are fabricated.

No private arrays or weights were loaded in this response. No project accounts,
Git commands, cloud resources, package installers or submission tools were used.
The public-source research does not establish the value of the new features.

Unit tests cover installer no-clobber behavior, source/evidence drift and source
versus notebook-output differences. New feature tests cover physical units,
masked poison, missing data, causality, stationary/coincident cases, permutation,
reflection, target exclusion and matched numeric ablations. Supplied model tests
cover initialization, input masks, metric aggregation and interrupted continuation.
These are intended tests, not a claim that they passed.
